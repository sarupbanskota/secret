-if user_signed_in? and current_user.id == @seminar.user_id
	.col-md-12
		.col-md-3
		.col-md-6
			%h3 Edit Proposal
			%br
			= render 'form'
			%br
			
			= link_to 'View Proposal', @seminar
- else
	You are not allowed to access this page


